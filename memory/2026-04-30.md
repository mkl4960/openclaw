## 2026-04-30 19:56 EDT
- Updated `check_youtube_link.py` to improve duplicate detection:
  - Normalises YouTube URLs (removes query strings, fragments, trailing slash).
  - Adds `last_notified_link.txt` safeguard to avoid reposting the same link consecutively.
  - Ensures processed email IDs are always recorded, stripping any surrounding `<` `>`.
  - Maintains atomic writes for `posted_links.json` and caps cache at 50 entries.
- Confirmed that these changes should prevent the intermittent extra Discord notifications reported on 2026‑04‑29 (four notifications after midnight).
- Next actions if duplicates persist:
  1. Verify Discord bot **ClawBot#0678** has `Send Messages` and `Read Message History` permissions in channel `148440279405533128` via a silent test payload.
  2. Check any external posting component that might bypass the script’s cache.
  3. Review cron logs for race conditions or lock‑file issues.
- Reminder: Do not modify reference files (`MEMORY.md`, `SOUL.md`, etc.) during this flush.
