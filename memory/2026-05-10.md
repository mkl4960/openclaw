## 2026-05-10
- Tried to start OpenClaw browser (profile "openclaw") to screenshot www.clgnj.org.
- Chrome failed to create `SingletonLock` in `/home/mlau/.openclaw/browser/openclaw/user-data/` (permission denied).
- Attempts to start with `noSandbox` via browser tool schema failed due to validation errors.
- Browser tool entered cooldown after multiple launch failures.
- Next actions: investigate and adjust file permissions or set `browser.noSandbox: true` via gateway config, or fallback to alternative screenshot method (e.g., web_fetch).
